{
  "name": "placitum",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Capability-secure shell language",
  "scripts": {
    "check-deps": "node scripts/check-deps.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "ci": "npm run check-deps && npm run typecheck && npm run lint && npm run test"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "8.57.1",
    "typescript": "~5.6.3",
    "vitest": "^2.1.9"
  }
}
